# Validation Quickstart: New Project

## Safety and prerequisites

Use `.worktrees/new-project-screen` and the Nix dev shell. Never point migrations, seeds or browser mutations at the real imported `horae` database or an existing user's preview. Keep ports 8080/8092 and their processes untouched.

The existing browser runner `crates/horae/tests/browser/run-design-checks.sh` creates its own temporary PostgreSQL cluster and refuses an occupied port. Extend that runner for this feature rather than silently reusing a running database. For integration development, provision a separate throwaway PostgreSQL cluster/database and set DATABASE_URL explicitly; role needs CREATEDB.

## Build and focused checks

From the worktree, inside `nix develop`:

```sh
cargo test -p horae-core
cargo test -p horae --features server project_creation
cargo test -p horae --features server invoice
cargo test -p horae --features server time_entries
cargo test -p horae --features server budget
cargo sqlx prepare --workspace -- --features server --all-targets
SQLX_OFFLINE=true cargo clippy -p horae --features server -- -D warnings
cargo check -p horae --features web --target wasm32-unknown-unknown
nix fmt
git diff --check
```

Run database commands only with the isolated DATABASE_URL. Regenerate the SQLx cache with server features; omitting them can delete it.

## End-to-end scenarios

1. Admin → Projects → New project. Create a client, choose it, add name/code/dates/tags; save and reopen details. Verify one project and all relationships.
1. Filter Projects by the saved tag, then record time on that project and filter Reports by the same tag. Verify grouped/detailed rows and CSV/XLSX downloads agree. Pending or failed filter requests must not display stale rows; retry restores the selected report. Archived history remains filterable and multiple tags never duplicate hours.
1. Exercise every type/rate/budget branch with real catalog tasks/team, including Add everyone, restricted tasks and zero rates. Verify independent expected totals and invoice eligibility.
1. Change draft values, await real save acknowledgement, reload; simulate failed save/conflicting tab/repeated final submit. Verify no lost acknowledged input or duplicate project.
1. Test managers, leads, assigned members and foreign-org users directly against server/compatibility/export paths. Verify absent confidential fields and unchanged personal timesheets.
1. Prepare fixed-fee single/milestone/monthly invoices; verify no synthetic time/double charge. Override terms/PO/taxes/discount; confirm snapshot isolation and PDF/CSV/XLSX totals. Void and regenerate to test occurrence claims.
1. With isolated mail stub, cross a budget threshold twice and retry a failed delivery. Verify one logical notification, stable identity and no retry after acknowledgement. Without configuration, verify disabled email control and explanation.
1. Inspect real application at widths 390/768/1440 and keyboard-only operation. Exercise each modal, error, empty and pending state. Do not execute/render prototype files.
1. Run existing Projects selection/bulk recovery, Clients, Timesheet, Invoices, navigation and menu regression suites, then full flake CI.

## Completion evidence

Record commands/results and outstanding limitations in the PR. Check off tasks only after their acceptance checks pass. Requirements checklist completion is not implementation completion. Final gate: complete workflow, adversarial review, supported build/test checks and a scoped PR ready for human review; no automatic merge.

## Optional budget mail validation

Deployment instructions are in [README — Budget alert email](../../README.md#budget-alert-email-optional).

Production configuration uses `HORAE_SENDMAIL_PATH` (absolute executable path, no arguments) and `HORAE_MAIL_FROM` (plain ASCII mailbox). Both are unset by default; a partial or invalid configuration rejects startup. The existing mail service must support `-i -f FROM -- TO`. No SMTP credentials or shell command are accepted by Horae. See `.env.example` for the supported address subset and limits.

On NixOS, supply these through `systemd.services.horae.environment` or the existing environment file. The transport must work as the service user under `NoNewPrivileges`, `ProtectSystem` and `ProtectHome`; a mail wrapper requiring privilege elevation or a home-directory configuration will not work with that hardening. Configure a compatible unprivileged transport separately; enabling budget email does not weaken the service sandbox.

Delivery has a 20-second deadline and at most five attempts. Stdout/stderr are discarded. An exit-zero acknowledgement means acceptance by the configured transport, not final receipt; a crash after acceptance can cause a repeated delivery with the same Message-ID. Unauthorized/inactive recipients and disabled/archived projects are not sent pending messages. Failed events are retained rather than reported delivered.

Operators can inspect statuses using their administrative database connection:

```sql
SELECT id, attempts, delivered_at, failed_at, last_error
FROM horae_outbox
WHERE event_kind = 'budget_email'
ORDER BY created_at DESC;
```

Tests must never use deployment mail configuration: `notifications::tests` constructs temporary executable stubs, and the browser runner explicitly unsets both mail variables. Do not run a real transport as a test fixture.

## Execution log — 2026-09-22

- Catalog-currency regression: a real CSV import followed by New Project finalization first failed the expected rejection, accepting an 80 USD catalog rate as 80 EUR. Migration 0038 now records known source currency without backfilling existing catalog units or changing project-task snapshots. Creation and name-based reuse, task enablement, native catalog amount changes, authorized options and rollback-only import checkpoints use that denomination. Unknown/incompatible rates require explicit project amounts, including zero; renames/no-ops do not stamp unknown units. The UI reuses its existing input/error links and requests a rate instead of suggesting an incompatible amount.
- Catalog verification passes 732 server-bin tests (11 pre-existing ignored), then the separately added checkpoint test; 109 core tests, 36 integration tests, 42 screen tests and 20 navigation tests also pass. Strict all-target Clippy passes both online and offline, WASM checking and the full Dioxus build pass, and SQLx preparation retains 887 entries: 18 new and exactly ten replaced runtime queries removed, with no unchanged metadata modified. The initial UI compile error was an incorrectly placed local placeholder calculation and was corrected before these gates. Only the isolated database received migration 0038; no imported database, preview, dependency or shared CSS changed.
- The catalog browser checks pass known USD inheritance in an EUR workspace, unknown-zero rejection, incompatible-currency field focus and explicit correction, alongside the previous task-rate/editor scenarios at three widths. Eight other screens match the original computed-style baseline. The first added recovery fixture incorrectly expected automatic saved status after a validation rejection; it now verifies the intended attention state until explicit resubmission. Its test catalog is restored after denomination scenarios to avoid changing later suites' defaults. Full-feature navigation/transport, client-default mutation review, flake and PR acceptance remain open; completion remains 43/54.
- Browser integration diagnostics: running task-rate checks before New Project broke the latter's seed-project count (16 instead of 3); the standard runner already puts task-rate checks last. In that standard relative order, the complete New Project and invoice-preparation suites passed, but the legacy-editor assertion then exposed that invoice preparation configures the seeded project. The editor test now creates its own unconfigured UUID-v7 fixture, with quiet psql output so the returned UUID excludes the command tag. The final `invoice-preparation project-task-rates` run passes together (`/tmp/horae-catalog-currency.JTH5jB.log`); the preceding complete creation/report/export/invoice run is recorded in `/tmp/horae-catalog-currency.R6p34c.log`. No assertion was removed and no application behavior changed for these fixture corrections. This is focused integration evidence, not a new passing unfiltered ten-suite or full-flake run.
- Existing-editor UI integration now consumes a manager-only capability projection checked against the current database role and organization. Pending/failed reads and project switches expose no stale fields; explicit retry restores them. Configured type/currency, unused rates and scoped budget totals are disabled with explanations. Saves disable local controls and preserve values on failure. Shared components/CSS defaults, dependencies, migrations, imported data and running previews are unchanged.
- Editor regression evidence: after correcting a test locator for the existing Actions label, the previous browser build failed because configured Type remained enabled. The server test initially failed to compile at the missing helper, then passed 19 configuration cases with current admin/manager/member roles, deactivation and tenant/missing-reference checks. The final focused browser run passes configured/legacy editing, task/person budgets, exact/zero values, failed reads/saves, project switching and keyboard retry at 390/768/1440. Actual editor captures at 390 and 1440 in `/tmp/horae-project-task-rates.VlBNDq` were inspected.
- Editor build gates pass: 729 server-bin tests (11 pre-existing ignored), 20 navigation tests, strict offline all-target server Clippy, WASM checking and the complete Dioxus build. SQLx preparation produces 879 entries, adding one runtime query with no existing metadata changed or removed. T023 remains open for global catalog denomination; completion remains 43/54 and no whole-feature/flake/PR completion is claimed.
- Browser date diagnostics: crossing midnight added a second seeded project to Reports' default date range, changing two measured page heights without a CSS change. The style fixture now pins Reports to the seeded Monday; all eight screens at three widths match the original baseline without replacing it. The creation/report/invoice fixture now uses the saved entry's actual date rather than assuming the timesheet Add action chooses Monday. An unfiltered run passed styles and Projects but stopped at `responsive-layout.cjs`'s login-navigation timeout. This is not a passing full runner and does not resolve the existing navigation/transport gates.
- The final isolated `new-project invoice-preparation` runner passes, including keyboard/responsive traversal, durable drafts, actual-date report/CSV/XLSX results, invoice defaults, failed/lost responses, concurrent tabs and mixed fee preparation. Its complete diagnostic log is `/tmp/horae-editor-browser.nIQngO.log`. An earlier focused run stopped after row validation and before the report/invoice assertions; its cause is not established by this passing rerun. The complete ten-suite runner and whole-feature transport/flake/adversarial acceptance remain pending.
- Existing-project mutation audit: a failing regression reproduced the legacy editor accepting an EUR-to-USD relabel on a configured project and also saving the invalid request's name. `update_project_record` now locks the project, reads/share-locks its current settings and rejects independent type/currency changes, absent required project rates, unused project-rate changes and changes to scoped budget totals/units. Project-wide budgets reuse core type validation. Missing settings preserve legacy edits; equivalent currency spelling preserves the canonical value. This is a consistency guard, not a new configuration migration/editor.
- Four new regression tests cover seven invalid hourly edits, eight compatible/idempotent edits, fixed-fee/non-billable preservation and a real transaction interleaving. The latter inserts configuration while holding the project lock, then proves a waiting edit rechecks it before writing. Compatible exact/zero rates reach project spend: a 60-minute entry remains 8,000/9,000 cents in task/person mode and becomes exactly 0/8,025 cents after explicit project-rate changes. The focused project module passes all 56 tests; the complete server run passes 728 tests with 11 pre-existing ignored, and all 109 core tests pass.
- Cache/build verification for this increment: preparation against the isolated database produces 878 entries, adding eight test queries and one runtime query without changing/removing any existing entry. Strict offline all-target server Clippy, WASM checking, the final four-test offline regression run, explicit-file formatting and whitespace checks pass. No UI, shared CSS, dependency, migration, imported database or preview changed; no new browser or full-flake completion is claimed. T023 still includes mode-aware editor presentation/browser recovery and global catalog denomination; all final feature gates remain required, with 43/54 tasks complete.
- Task-rate UI integration: Project Detail now uses current authorized metadata to offer the existing shared input only for eligible billing modes. A 36-case current-role/type/mode matrix verifies both the currency and omission from unauthorized serialized responses. Pending/failed metadata hides mutation controls; already-linked task choices are disabled. Failed enablement preserves selection and amount, while successful enablement clears both. Adversarial review added explicit-rate rejection for legacy non-billable projects without changing their previous catalog inheritance.
- Regression evidence: the metadata test first reproduced the absent currency projection, and the real creation-to-detail browser flow failed at the missing rate input on the prior available build. Browser fixture corrections scoped catalog lookup to Demo Org after the bulk suite added another organization and matched the existing Non-Billable label; neither required a production change. The final Dioxus build and isolated `project-bulk-actions menu-popovers project-task-rates` runner pass. Tests cover USD/EUR rejection and compatible inheritance, exact positive/zero amounts, validation errors, disabled pending controls, preserved failed requests and keyboard retry at 390/768/1440. Eight other screens match their computed-style baseline at all three widths. Final screenshots at 390 and 1440 were visually inspected for layout and overflow.
- Final task-rate increment checks pass: 724 server-bin tests (11 pre-existing ignored), 20 navigation tests, strict offline all-target Clippy, WASM checking and SQLx preparation with all 869 entries and only two runtime-query replacements. The new browser suite is included in the default runner. No shared CSS/control defaults, dependencies, migrations, imported database or existing previews changed. Completion remains 43/54: T023 still requires the global catalog-denomination and existing-project-edit audits, and whole-feature transport, conditional coverage, adversarial, full-flake and PR gates remain open. This focused run is not a new unfiltered browser/full-flake completion.
- All 20 detail-navigation tests pass with the optional rate argument. Whitespace checks also pass. Completion remains 43/54 tasks; the task-rate input/browser path is the next integration step, not a finished user workflow.
- Cross-currency task-enablement regression: enabling a task with an 8,000 EUR catalog rate on a configured USD task-billed project succeeded before the fix. The shared server helper now rejects incompatible inheritance before inserting a link. The manager-authorized operation accepts an optional exact-text amount and displayed currency; it rejects malformed/negative/overflow amounts, mismatched current currency and modes that do not use task rates. Existing links remain idempotent, including zero overrides; new-task creation still uses absent defaults. Four new tests cover rejection without links, exact explicit recovery (zero, 80.25 and the maximum supported amount), repeated enablement, seven invalid inputs and four unused-rate modes. All 51 project/privacy/mutation tests pass. SQLx preparation produces 869 entries, with three new test queries and replacement of the runtime query; strict offline all-target Clippy, WASM checking and explicit-file formatting pass.
- This is not completion of T023: the current Project Detail caller supplies no explicit rate. Its mode-conditioned, currency-labelled input and actual browser recovery remain required, together with the global catalog-denomination and existing-project-edit audits. No UI/CSS redesign, migration, dependency, real database or existing preview was changed; no new full-suite/browser/flake completion is claimed.
- The task-enablement increment also passes strict all-target server Clippy with `SQLX_OFFLINE=true`, explicit-file formatting and `git diff --check`. Completion remains 43/54 tasks; final acceptance gates are still open.
- Task-enablement rate-mode checkpoint: the regression reproduced a configured person-billed project copying an unused catalog task rate. The shared enablement query now inherits catalog rates only for configured task-billed time-and-materials projects, while projects without settings retain legacy inheritance and existing links retain their overrides. All ten project-module tests pass, including a 36-case matrix of project types, rate modes and absent/zero/positive defaults. SQLx preparation against the isolated database produces 866 entries: three new test queries and replacement of the changed runtime query, with no unrelated cache removals. This is a partial T023 correction, not completion of cross-currency task enablement, catalog denomination or existing project editing. No CSS, browser, dependency, migration, real database or preview changes were made; no new full-suite/browser/flake run is claimed.
- T021 is complete after configured-import verification. The final offline gates pass: 36 integration, 41 screen and 20 navigation tests, strict all-target server Clippy and WASM checking. SQLx preparation re-evaluated the application's source and both query-bearing integration targets by updating only their timestamps; all 863 entries are present, with five new test queries and no existing changes/deletions. No application behavior, CSS, dependency, handoff, real database or preview changed. T023/T030 and the remaining browser/adversarial/flake/PR gates remain open; completion is 43/54 tasks.
- Configured-import preservation: a new finalization-to-import regression covers eight combinations of CSV/normalized API rows and task/person/project billing or fixed fee, each through preview, commit and retry. It compares all 16 catalog/configuration collections before and after, including private notes, completed draft, memberships, cost overrides, scoped budgets, task grants, tags, milestones and invoice defaults. Remote names, active flags, dates and GBP rates do not overwrite local USD settings or EUR-denominated costs. The restricted member's time is imported through privileged authority without granting tracking, financial or project-progress access; imported billed flags do not claim a Horae invoice. Known 75-minute totals remain 10,000/6,875/8,250/0 USD minor units and 2,813 EUR minor units cost. The fixture's initial incorrect milestone-table name was corrected before the focused test passed; no production behavior was changed. The full run passes 109 core and 718 server-bin tests, with 11 pre-existing ignored server tests. This tests the shared API row engine, not a new live Harvest HTTP/account import.
- Imported-rate compatibility: the first regression run reproduced a new project-task link accepting an 80 EUR source rate in a configured USD project. The shared row resolver now checks the source currency (organization default only when absent) before inserting a configured override. Existing links keep their stored overrides, including zero, and projects without settings retain their legacy import behavior. A rejected row rolls back its link/catalog changes; a corrected following row succeeds without polluted lookup state. No conversion, migration or existing-rate rewrite is performed.
- Four new database tests cover the CSV importer through project spend, reports, invoice generation and Harvest list/detail responses. The legacy fixture remains exactly 165 minutes, 13,000 EUR minor units billed and 3,300 EUR minor units cost, with rates 8,000/0/3,000 and no project settings. Reimport creates no duplicates. A configured USD recovery fixture invoices exactly 8,000 USD minor units after rejecting the incompatible row. The focused three-test run and subsequent full run pass: 109 core tests and 717 server-bin tests, with 11 pre-existing ignored server tests.
- Build/cache gates: warm-target SQLx preparation emitted partial metadata, first omitting integration queries and then omitting cached application queries. The offline attempt was stopped rather than accepting those deletions. Cleaning only the `horae` package's dev artifacts in the isolated `/tmp` target and preparing with `--workspace -- --features server --all-targets` restored all 858 entries: nine additions, no existing modifications/deletions. With the complete offline cache, 36 integration, 41 screen and 20 navigation tests pass, followed by strict all-target server Clippy and WASM checking. Explicit-file formatting and whitespace checks pass. No browser rerun or full flake completion is claimed for this backend-only increment.
- Compatibility audit remains open under T021/T023/T030: verify that importing into an existing configured project preserves its complete settings/assignment/privacy graph. Also trace global catalog defaults without currency metadata and the old task-enablement/project-edit mutation paths before claiming complete denomination safety. These are audit findings, not reproduced failures or a completed redesign of existing project editing. Browser/transport recovery and final flake acceptance remain outstanding.

## Execution log — 2026-09-21

- T053 deployment documentation is complete: README and DESIGN now describe the actual creation route and optional mail transport, including disabled/invalid configuration, service-user restrictions, bounded retries, sanitized failures and acceptance-versus-receipt semantics. The diagnostic SELECT was executed read-only against the isolated schema. Mail configuration/delivery tests are included in the passing 713-test server run. Explicit-file formatting and whitespace checks pass; `git diff 554e140 -- design/project` is empty. No real service configuration, mail delivery, dependency or imported data changed. This documentation checkpoint does not complete final acceptance or authorize the pending transport backport.
- T032 completion audit added two database regressions around the existing transactional implementation. Catalog reuse/new tasks, duplicate grant collapse, empty restrictions, concurrent retries and project-only overrides all pass; complete pre-existing profile/assignment/catalog snapshots are unchanged. Thirteen invalid graph cases preserve the exact saved draft and leave project/task/tag/event counts unchanged, then a corrected request succeeds. SQLx preparation with `--workspace -- --features server --all-targets` and `CARGO_INCREMENTAL=0` produced 849 cache entries: seven additions for tests, no existing modifications/deletions. All 109 core and 713 server-bin tests pass (11 pre-existing ignored), including existing task-access, approval/import, mail, invoice and export fixtures. No runtime behavior, migration, dependencies, shared CSS, imported data or existing preview changed.
- Assignment-audit finishing gates passed 41 screen and 20 navigation tests, strict all-target server Clippy with the offline cache, WASM checking, explicit-file no-cache formatting and `git diff --check`. This test-only runtime increment did not rerun the browser; the preceding `cb6c8c7` browser evidence still applies to unchanged production UI. T032 is complete, while role-browser coverage, field-error recovery, imported currency audit, interrupted response-body handling and final flake/PR gates remain open.
- Assignment validation TDD: the new server test reproduced a rejected task rate without a row identity. Typed errors now identify task rates/budgets by draft-row UUID and person billable rates/costs/budgets by user UUID, including the row that overflows an aggregate fee budget. Eleven invalid-input cases run against a two-row fixture. All seven validation tests, 41 screen tests, 20 navigation tests, strict all-target server Clippy, WASM checking and the full Dioxus build pass. The existing Input error-link option is reused; shared components, CSS defaults, accepted values, authorization and draft schema are unchanged.
- Assignment browser verification: `project-bulk-actions menu-popovers new-project invoice-preparation` passed in one fresh disposable database. Fifteen new field/width cases verify error links, focus above the footer, intact invalid values, correction/retry and an unchanged first task when the second is rejected. Captures in `/tmp/horae-assignment-errors.NSj4dy` were inspected at 390/1440. Eight other screens match the existing computed-style baseline. Real creation, report/export consumers, invoice defaults, concurrent saves and idempotent finalization continue to pass. Targeted formatting and whitespace checks passed. No SQL/cache, migration, dependency, imported database or running preview changed. T046/T048 and final feature gates remain open; this does not resolve prior reload/transport instability.
- Billing field-error TDD: after correcting a missing UUID import in the new fixture, server tests reproduced absent project-rate and milestone-row identities; the browser reproduced the missing rate error association. Typed rejections now cover billing choices, project rates, total budgets, alert configuration/percentage, single/monthly fees and milestone name/date/amount. Row identities are UUIDs, not labels or positions; malformed UUID targets are rejected. Empty schedules focus Add milestone, overflow targets the offending amount, and inactive inputs remain untouched. Checkbox identity/error attributes are optional with preserved default state/classes. All six validation tests, 41 screen tests, 20 navigation tests, strict all-target server Clippy, WASM check and full Dioxus build pass.
- Billing browser verification: New Project passed on the first post-change run, but the subsequent bulk suite expected two seeded projects after creation had added a third. Inspection confirmed the fixture-order dependency; no assertion was weakened. The final `project-bulk-actions menu-popovers new-project invoice-preparation` run passed in a fresh isolated database, covering errors/recovery at 390/768/1440, a second milestone without changing the first, unavailable mail correction without delivery, pending-save/finalization retries, real permissions/bulk mutation and invoice defaults. Eight other screens match the computed-style baseline. No SQL/cache, migration, CSS, dependency, imported data or existing preview changed. T046/T048 still require per-task/person errors and remaining whole-screen cases; prior reload/transport instability and full-feature/flake gates are not resolved by this run.
- Basic field errors: new server/browser checks first reproduced missing field identities and absent accessible error links. Name, code, planning dates, currency and private notes now reuse the typed rejection path, without changing validation rules or draft payloads. All three server validation tests and 39 screen/20 navigation tests pass; default/opt-in Textarea and SelectField rendering is covered. Strict all-target server Clippy, WASM check and full Dioxus build pass.
- Browser validation on this increment: a first run reproduced the existing 30-second reload/load-event timeout before reaching the new field cases, with no pending API calls or page errors. Pending-resource diagnostics were added without suppressing failures or changing waits. A subsequent run caught an incorrect date label in the new test fixture; it now uses the calendar's existing accessible name. The final `new-project menu-popovers invoice-preparation` runner passed, including 12 basic-field/width cases, 18 invoice-field/width cases, initialization/recovery without replacement writes and the downstream creation/report/export/invoice/idempotence flows. Eight other screens match the existing computed-style baseline at three widths. The successful rerun does not resolve the prior reload timeout or the separate interrupted-body regression; T051 stays open. No SQL/cache, migration, dependency, shared CSS, imported data or running preview changed.
- Field-error recovery follow-up: the new assertion reproduced Save remaining disabled after a definite validation rejection. Explicit Save/Cancel/back/confirmed Discard now clear that known rejection before using the existing serialized intent path; unrecognized/uncertain failures retain their original retry guard. The final New Project browser run passes all 18 field/width combinations, repeated rejection, Cancel/reopen with invalid input, corrected Save followed by a lost creation response, and the original idempotence/concurrency/report/export/invoice flow. All 38 screen and 20 navigation checks, strict all-target Clippy, WASM and Dioxus builds pass again. Eight other screens match the unchanged style baseline. No SQL/cache, migration, dependency, shared CSS, imported database or existing preview changed. T046 remains open for the rest of the form; no full-feature or flake completion is claimed.
- Invoice field-error TDD: server and browser tests reproduced missing structured field identity and absent `aria-invalid` on a rejected custom payment term. Creation rejections now identify the six invoice-default fields, and the form links each to a visible inline message without echoing raw input. Known 400 fields receive focus after the form unlocks; unknown/transport/conflict responses do not supply focus targets. Shared Input gains only optional error attributes; defaults and CSS remain unchanged. The first SSR fixture failed because it constructed an event handler outside a Dioxus runtime; using the existing VirtualDom pattern fixes the fixture. Two validation, 38 screen and 20 navigation checks pass, as do strict all-target Clippy, WASM and Dioxus builds. The real New Project/Invoice Preparation browser run and eight-screen style comparison pass; all six errors preserve values, link messages, focus visibly above the footer and recover at 390/768/1440. Captures in `/tmp/horae-project-field-errors.ICFLkP` were inspected at 390/1440. A subsequent check for a more direct Save recovery path failed earlier on a 30-second reload timeout, before reaching its new assertion; that failure's cause is not established. Whole-form error coverage and final gates remain open.
- Independent mobile follow-up passed content width, keyboard toggle/Escape/focus, navigation close, short-viewport account access and desktop width persistence across breakpoints. It completes the previously unexecuted suite without changing the failed menu/transport result.
- Typography/strip verification: strict all-target server Clippy, WASM checking, full Dioxus build, targeted formatting and whitespace checks passed. New Project and Projects browser suites passed, including durable creation/recovery and shared-icon default assertions; eight other screens retain identical computed styles at three widths. Actual-app heading, information strip and compact invoice captures were inspected in `/tmp/horae-project-final-styles.VMa1ph`. The subsequent menu suite passed its interaction assertions but failed its page-error gate on the already recorded Dioxus 0.7.9 `magic.rs:267` response-body unwrap panic at `/`; the combined run is failed and did not reach mobile navigation. No error was suppressed or dependency backport applied. No SQL/cache, migration, real imported data or existing preview changed; completion remains 40/54 and T019/T048/T051 stay open.
- Final typography/strip TDD: a browser assertion first reproduced 12px instead of the mapped 10px task-information padding. The strip now uses a theme-aware primary wash, a decorative information glyph, 8px gap and the light divider token. Header/form/invoice separators, invoice title margins, uppercase tracking, removal hint colors and the compact Back control use opt-in utilities. The Back arrow is hidden from its accessible name; navigation behavior is unchanged. A new SSR test preserves default navigation-icon dimensions and verifies explicit sizing. All 36 screen and 20 navigation tests pass. This is a visual increment, not completion of the conditional/error-state or transport gates.
- Complete browser checkpoint: the final milestone/selector build passed the unfiltered nine-suite runner: Projects design, responsive layout, menu popovers, mobile navigation, bulk actions, bulk recovery, action errors, New Project and invoice preparation. This includes real Clients/Timesheet/Invoices smoke checks and native keyboard/callback regression checks. The eight-screen/three-width style comparison also passed. Shared-control diff review confirms optional Input additions and opt-in selector/calendar behavior without replacing default callers. T047/T049 are complete (40/54 total); this green run does not resolve prior intermittent failures or the deterministic Dioxus response-body panic, which remains under T019/T051. No claim of full flake or feature completion is made.
- Fee-layout/selector TDD: the browser regression first reproduced the native budget SELECT instead of the shared selector. Budget, monthly invoice day and payment terms now reuse SelectField, preserving type-dependent choices, custom terms and focus return. Milestones now have flexible name + 160px date + 140px exact amount + 40px removal columns, with token-backed 10px row and horizontal input padding. The first responsive check then exposed an unreadably narrow name at the sidebar transition; stacking below 900px fixes it without changing the shared sidebar or form controls. Named row groups, field labels, native date segments and keyboard removal remain accessible. Removal returns focus to Add milestone; its glyph uses the muted design color.
- Fee-layout verification: 35 screen tests and 20 detail-navigation tests passed, including non-billable budget choices; strict all-target server Clippy, WASM checking and full Dioxus builds passed. The focused New Project browser workflow passed exact milestone totals, keyboard traversal/removal, ISO date and amount recovery, hidden monthly-day preservation and the existing real creation/report/export/invoice flows. Geometry/focus checks cover 390/768/769/900/1180/1440; actual application captures in `/tmp/horae-milestone-controls.KFPN4c` were inspected at 390/769/1440. Eight other screens retained identical computed styles at three widths. No SQL/cache, migration, dependency, imported data or existing preview changed. Full feature completion, conditional field-error traversal and the previously recorded transport failure remain separate gates.
- Assignment controls: the browser regression reproduced the native teammate SELECT where the searchable shared control was required. An earlier attempt timed out during login, before reaching this assertion; its cause was not established. Team now reuses SelectField with pending/empty/error/retry states and disabled selected people. Task entry reuses Input Group at 40px with a 260px preferred basis, inline suggestions and Enter support. The explicit Add task button remains for touch/discoverability and returns focus to the input. Failed/pending catalog searches cannot add from stale results; draft values and current assignments survive retry. Shared component defaults remain unchanged; CSS adds only one width token and two opt-in generated utilities.
- Assignment-control verification: 34 screen tests and 20 detail-navigation tests passed, as did strict all-target server Clippy, WASM checking and the full Dioxus build. The New Project browser workflow passed teammate/task search, held responses, empty/error recovery, keyboard selection/focus return, duplicate protection, responsive picker bounds at 390/768/1440 and the existing persistence/creation/report/export/invoice scenarios. Computed styles on eight other screens at three widths matched the earlier baseline. No SQL/cache, migration, dependency, real imported data or existing preview changed. These focused gates do not resolve the previously recorded intermittent navigation/transport failures; T049/T051 and full flake verification remain open. Completion remains 38/54, with milestone layout, the task information strip, remaining selectors and complete conditional/error traversal still pending.
- Assignment-row TDD reproduced a 343.328125px cost input instead of the intended compact 120px field. Task and team rows now share a page-scoped grid that moves editable controls below identity on narrow screens, retaining native labels, currency/hour units, real access controls and compact removal. Team rows reuse the shared Avatar at a scoped token-backed 30px size and display the actual project role; the API does not provide the prototype's profile/job subtitle, so none is invented. Profile defaults are placeholders rather than saved values; private costs remain authorization-gated. No SQL, migration, dependency or shared-control default changed.
- Assignment-row verification: 33 screen and 20 navigation tests passed, including private-cost exclusion and profile placeholders; strict all-target server Clippy, WASM checking and the full Dioxus build passed. The New Project workflow passed conditional task/person rate and budget widths, zero/`12:30` recovery, native Tab visibility, real creation, reports/exports and invoice behavior at 390/768/1440. Actual task/team captures were inspected at 390/1440 in `/tmp/horae-project-rows.VxbUye`. Eight other screens retained identical computed styles at three widths. Initial reruns corrected a test locator's relative scope and a mistyped browser executable path; neither changed assertions or production behavior.
- The combined New Project and invoice-preparation suites passed, but the following mobile-navigation suite failed its final page-error check with `unreachable`. Its initial recorder lacked a stack/URL, so this run alone does not establish the cause. Diagnostic recording now retains those fields and bounded console errors without weakening the check. A standalone rerun passed, which does not resolve the intermittent failure or the separately proven Dioxus transport panic. T049/T051 remain open. Explicit-file formatting and whitespace checks passed; completion remains 38/54, with add/search controls, milestones and the final gates still outstanding.
- Footer TDD reproduced a focused planning-date trigger below the visible mobile form (bottom 899.5px while the footer began at 768px). The editor now has an independent scrolling panel and non-overlapping action row, using seven opt-in generated utilities, existing theme tokens and page-scoped structural selectors. No shared control default, shell component, dependency, query or database changed. Mobile navigation retains document scrolling while expanded; the desktop panel follows the shared sidebar width.
- The focused browser workflow passed after the layout change, including native Tab traversal at 390/768/1440 and short-screen navigation. The client selector fits a 320px-high viewport; its upward-placement/resizing check now uses a 420px-high viewport with the trigger explicitly below the midpoint, leaving room for the persistent footer. Earlier fixture attempts at 320/420 placed the trigger in the upper half, where filtered results correctly opened downwards; no placement assertion was removed. Strict all-target server Clippy, WASM checking and the full Dioxus build passed.
- Footer regression: 33 New Project and 20 detail-navigation tests passed. An initial mistakenly filtered server-bin invocation ran zero tests and is not counted. Computed styles remained identical on eight other screens at three widths; Projects and responsive-layout suites passed. The all-nine-suite run then failed in menu-popovers on the already identified Dioxus 0.7.9 response-body `unwrap` panic (`magic.rs:267`); no page errors were suppressed and T049/T051 remain open. The six remaining suites ran separately and passed: mobile navigation, bulk actions, bulk recovery, action errors, New Project and invoice preparation. New assertions verify non-overlap, viewport-sized document, sidebar-collapse geometry and short-screen navigation. Actual-app mobile/desktop captures in `/tmp/horae-project-footer.EJMrkf` were inspected. Explicit-file formatting and `git diff --check` passed. Completion remains 38/54; no real imported data or existing preview changed.
- Compact-field TDD first reproduced the payment-terms width mismatch (384px rather than 240px). Rates, total budgets, single/monthly fees, terms/PO and tax/discount controls now use opt-in token-backed widths, mono numeric alignment and wrapping inline groups. The shared `Input` retains its original default class/external-label behavior and accepts optional utility classes/accessible names. Two additional component checks cover unchanged defaults and native value/disabled state. No rate calculation, draft parsing, SQL/cache, migration or dependency changed.
- Actual-app captures exposed a mobile rate description compressed beside the amount despite no horizontal overflow. A new geometry assertion reproduced that failure before the 320px preferred label basis was added; the amount now wraps below readable text on narrow screens. Custom payment days remain editable with clear spacing, and keyboard removal of the second tax restores focus to the first tax. Final captures were inspected at 390 and 1440 in `/tmp/horae-compact-fields.HMH0lp`; these are not proof that the remaining footer/keyboard or full-screen alignment work is complete.
- Compact-field browser verification: computed-style comparison passed for eight existing screens at 320/768/1440. The final combined Projects design, mobile-navigation, New Project and invoice-preparation suites passed, including measured field widths at 390/768/1440, persisted values, recovery/concurrency, real report/export/invoice behavior and no page errors. An earlier combined run timed out waiting for `list_projects` in the existing held-spend navigation fixture, before New Project; additional failure diagnostics now expose its page/error state. The passing rerun does not prove that intermittent navigation or the separately reproduced Dioxus body-read panic is resolved. T046–T050 and final feature gates remain open (38/54 complete).
- Compact-field build gates: 33 New Project component and 20 detail-navigation/helper checks passed, strict all-target server Clippy and warning-free WASM checking passed, and the full Dioxus client/server build passed. Targeted no-cache formatting and `git diff --check` passed. No real imported database, deployment mail configuration or existing preview was used or restarted.
- Option-card TDD reproduced the missing type icons in both the component renderer and the previous browser build. Type cards now use the existing icon family in 32px tiles, and rate/visibility options use the handoff's 12px padding, 8px radius, input border and selected/focus rings. Native radios retain Arrow-key behavior. The additions are opt-in utilities and scoped `np-*` selectors; no existing control default changes. The complete source-to-component mapping is recorded under T045 (38/54), with unfinished geometry, compact fields, task/team rows and accessibility acceptance explicitly retained under T046–T050.
- Option-card finishing checks: warning-free WASM checking passed. Explicit-file no-cache formatting processed nine Rust/shell/Spec Kit files, changing only Rust line wrapping in the utility registry and new test; `git diff --check` passed. The shared-style baseline lives outside the repository and can be reproduced with `HORAE_STYLE_RECORD` before rebuilding, then `HORAE_STYLE_BASELINE` against the new build. Full Dioxus artifacts exercised by the browser suites include the option-card changes.
- The new browser check initially failed because its case-sensitive selector spelled the actual `Non-Billable` label incorrectly; that selector was corrected, with no application behavior or assertion removed. The combined Projects design, mobile-navigation and New Project suites then passed, including keyboard option selection, exact card dimensions, durable draft recovery, report/export integration, invoice preparation and draft editing. Computed-style comparison against the pre-change build passed for eight other screens at 320/768/1440. All 31 New Project screen and 20 detail-navigation/helper tests and strict all-target server Clippy passed. This does not complete the full feature's visual, transport, adversarial or flake gates. No query/cache, migration, dependency, real imported data or running preview changed.
- Invoice-interaction TDD reproduced missing exact-value form helpers and the absent review/generate-draft controls in the previous browser build. Preparation now inherits or explicitly resolves project defaults, supports project selection, hides stale totals, disables generation until values match the reviewed request, and guards pending local actions. Draft-only edits reuse the same controls and preserve entered values after failures. Preview and frozen invoice/export totals share the component projection. No shared CSS, dependency, migration, SQL query/cache, real imported data or existing preview changed.
- Interaction verification: all 703 server-bin tests passed (11 pre-existing ignored), plus 20 detail-navigation/helper and 30 New Project screen tests, strict all-target server Clippy, warning-free WASM checking and the full Dioxus client/server build. The actual creation → time entry → invoice workflow verifies inherited custom terms/two taxes, PO/project overrides, stale/invalid review rejection, duplicate-click protection, save-error recovery, second-tax editing, persisted draft changes and no edit controls after sending. A separate disposable-database fee fixture verifies mixed-default resolution, empty-selection errors, failed-preview recovery, no preview writes/materialization, fee rows without invented hours/rates, widths 390/768/1440 and claiming only selected projects. Both browser suites passed together on the final build.
- Browser failures remain recorded: the first fee fixture used the wrong `users.role` column and was corrected to `org_role`; an initial standalone start timed out before the client field appeared, and the fixture now waits for the actual client-list response before opening the form. One later New Project run timed out on `page.reload()` during the pre-existing autosave case, before reaching invoices. The subsequent diagnostic run passed, but this is not evidence that the reload instability or upstream Dioxus transport panic is fixed. No page errors or assertions were suppressed. T042 is complete (37/54); T043 remains open for clarification of individual fee-row editing versus the contract's preservation of source amounts. Full shared-screen/flake/adversarial gates and the reviewed PR remain required.
- Preparation screen-regression and format gates: all 12 detail-navigation and 30 New Project screen checks passed; explicit-file no-cache formatting passed for the twelve changed source/contract/task files and `git diff --check` was clean. These are not browser interaction checks of the new preparation API, whose frontend remains pending.
- Invoice-preparation TDD first failed because the preview API did not exist. A manager-authorized `prepare_invoice` now reads a repeatable-read/read-only snapshot, shares time eligibility/rates and fee calendars with generation, and returns project defaults, source rows and exact optional adjustments. Conflicting selected defaults have no fabricated due date or total; explicit values resolve them. Existing occurrence identities prevent reoffering billed fees and preserve frozen released fees after schedule changes. No preview inserts invoices, claims time or materializes fees.
- Preparation verification: five new checks cover exact two-tax rounding against generation, selected/foreign/duplicate/oversized projects, explicit mixed-default resolution, no time/fee mutation, claimed/released fees, a deterministic concurrent-edit snapshot and 10,000/10,001-row boundaries. Existing milestone/monthly and selected-currency fixtures also exercise the preview. All 698 server-bin tests passed (11 pre-existing ignored), plus 109 core tests and strict all-target server Clippy. WASM compiles but currently reports three unused preview-DTO warnings because the preparation UI is not yet connected; do not suppress these instead of completing T043. SQLx preparation refreshed both query-bearing integration roots and produced 842 records (12 new, only the replaced fee query removed). No dependency, migration, shared CSS, imported data or running preview changed. T042 remains open for interactive integration, and browser/full-flake/final PR gates remain pending.
- Invoice-presentation gates: all 12 detail-navigation and 30 New Project screen tests passed, along with strict all-target server Clippy, WASM compilation and explicit-file no-cache formatting. SQLx preparation initially omitted 93 unchanged integration-test entries even with incrementality disabled; refreshing the two query-bearing integration roots' timestamps before repeating preparation restored all 831 entries. Only the replaced invoice preflight and CSV fixture queries changed. No shared CSS, dependency, imported database or running preview changed. T044 is complete (36/54); preparation/edit controls, the unapproved Dioxus transport backport and final browser/flake/PR gates remain open.
- Invoice presentation TDD reproduced the missing subtotal/adjustments in the actual Dioxus detail page. UI, CSV, XLSX and PDF now share a projection of saved invoice components, with invoice-owned payment terms and purchase order. CSV preserves exact bigint cents and quoted/multiline metadata; fee rows retain absent quantities. XLSX keeps ordinary amounts numeric and uses exact text beyond Excel's precision. The real Typst layout is inspected for both taxes, discount, payment terms and PO, while the existing real-PDF determinism and export snapshot/limit tests remain active. Full server-bin verification passed 693 tests (11 pre-existing ignored); this checkpoint does not claim browser or full-flake verification.
- Invoice-default TDD reproduced Net 30 instead of the saved 21 days and silently accepted mixed project defaults. Generation now copies compatible project defaults or requires explicit values, filters both time and stored/materialized fees by selected projects, and snapshots checked discount/tax components. Draft-only edits share the invoice state lock with send/void and preserve source lines and project settings. This is backend integration, not completed preparation/UI/export presentation.
- Invoice-default verification: all 692 server-bin tests passed (11 pre-existing ignored), including eight new checks for exact two-tax rounding, receipt/custom terms/PO, mixed-default resolution, unavailable/duplicate selections, fee-only adjustments, frozen historical invoices, overflow rollback and a deterministic edit/send race. A migration test creates legacy data before 0037 and verifies preserved dates and maximum bigint totals afterward. The additive migration was applied only to the isolated `horae_new_project_fees` database (37 migrations); no previous checksum, imported data or preview changed. Strict all-target server Clippy, WASM and all 11 navigation/30 New Project screen tests passed. T040/T041 are complete (35/54); T042–044 and final feature gates remain open.
- Invoice-default cache/format gates: the first incremental SQLx prepare omitted unchanged integration-test metadata. Repeating `cargo sqlx prepare --workspace -- --features server --all-targets` with `CARGO_INCREMENTAL=0` regenerated all targets: 831 records, 27 new and only the seven replaced production queries removed. No existing metadata record changed. Targeted no-cache formatting passed for thirteen Rust/Spec Kit files without changes, and `git diff --check` passed. This backend checkpoint does not claim a Dioxus/browser/flake rerun or completed invoice presentation.
- Report-tag TDD: server tests first failed at the missing filter contract; the extended browser workflow then failed against the previous build at the absent Project tag selector after successfully creating and tracking to the new project. The implemented UUID filter now intersects all existing filters before grouping, streaming CSV or checking/rendering XLSX. Three database regressions verify dimensions/details, archived history, cross-organization isolation, unknown IDs, multiple tags without duplicate rows and oversized excluded entries.
- T015 acceptance passes: all 684 server-bin tests (11 pre-existing ignored) and the combined Projects design/bulk/recovery/action-error/New Project browser run passed. The latter creates a real project and time entry, checks filtered summary/detail rows and both downloads, and verifies pending/error/retry behavior without stale rows. SQLx preparation succeeded against the isolated 36-migration database with 811 entries (six new, exactly three replaced queries removed); the full Dioxus build passed. No CSS, dependency, migration, imported-data or running-preview change was needed. Final feature-wide browser/flake gates and the unapproved response-body fix remain outstanding.
- Report-tag finishing gates: strict server Clippy with all targets, WASM compilation, all 11 navigation and 30 New Project screen tests passed. Explicit-file no-cache formatting covered eleven Rust/Spec Kit files with zero changes; `git diff --check` passed. T015 is complete (33/54 implementation tasks), not the overall feature or final PR gates.
- Saved-details/tag checkpoint: backend tests first failed at the missing reads; a navigation test and the real creation browser workflow then reproduced the UUID-only detail placeholder. The detail now reads persisted basic fields and tags, with notes projected only for the current administrator role. Two database tests cover role changes, deactivation, tenant mismatch, ordinary/lead access, private/unlinked tags and retained shared labels. Eleven navigation/helper tests pass, including pending/failed route changes without stale metadata and tag UUID matching independent of label collisions.
- The Projects tag filter composes with search/client/status, clears bulk selection on change and resets with the other filters. Its choices contain only authorized project links; failed/pending reads never broaden an active tag filter. This is part of T015, not completion: report filtering and matching CSV/XLSX exports remain required. The detail addition is a basic read panel, not a general dashboard redesign. No task count is advanced by this checkpoint.
- A browser regression with a 1,000-character unbroken administrator note reproduced horizontal overflow at 390px. The generated `wrap-anywhere` utility is opt-in only on the saved-detail section; no existing CSS default was changed. SQLx preparation against the isolated 36-migration database succeeded with 808 entries (ten new, zero existing modifications/deletions). No migration, dependency, imported data or existing preview changed.
- Saved-detail verification: the full server-bin suite passed 681 tests (11 pre-existing ignored), and the Dioxus client/server build passed. The combined browser run passed Projects design, bulk actions, bulk recovery, action errors and New Project. The latter verifies actual persisted code/client/date/tags/private notes, the 390/768/1440 overflow regression, real tag filtering, bulk-selection clearing and filter reset, in addition to the existing recovery/finalization checks. This does not claim a fix for the separately reproduced Dioxus response-body failure or complete browser/flake acceptance.
- Saved-detail build gates: strict server Clippy including all targets and the WASM check passed. Explicit-file no-cache formatting and `git diff --check` passed. Shared Menu/Combobox/chip defaults remain covered by the green Projects browser suite; the only CSS addition is the generated opt-in wrapping utility.
- Basic-form TDD: new checks first failed on the missing shared tag field/form dropdowns and the code field's previous 384px width. Client/currency now reuse form/menu primitives in an opt-in `SelectField`; code uses the handoff's 160px token and tags use shared neutral chips. Tag tests cover Unicode length, case-insensitive duplicates, atomic capacity rejection and composition input. A slow-response browser check reproduced simultaneous loading/old-empty states; stale result footers are now hidden while pending. A short-viewport check reproduced a 28px upward-popover gap after filtering; an observer limited to open form selectors keeps the panel anchored as results change.
- Basic-form verification: all 30 screen tests and nine navigation tests passed, as did strict server/test Clippy, WASM compilation and the full Dioxus build. The focused New Project browser suite passed client search/error recovery, explicit/inherited currency, real client creation, tags, keyboard/date behavior, responsive widths, durable recovery, concurrent edits, lost acknowledgements, finalization and discard. Actual-app client/currency captures were inspected at 390 and 1440 in `/tmp/horae-basics-browser.qCe1je`. T014 is complete (32/54); tag consumers and full-feature acceptance remain open. No query, migration, dependency, real imported database or existing preview changed.
- Shared regression evidence: Projects design and mobile navigation passed, including unchanged Menu/Combobox and default chip colors/width. The combined run then failed on a 30-second `page.goto('/projects')` timeout in bulk actions after its initial keyboard-selection assertions. This is a failed suite, not a pass, and the output does not establish the timeout's cause. Independently, bulk recovery and action errors passed. Menu and responsive checks reached all their interaction/layout assertions, including Timesheet calendar behavior, but both failed their page-error gate on the existing Dioxus `magic.rs:267` response-body panic at `/`. No errors were suppressed, no backport was applied and the overall browser gate remains failed. Targeted no-cache formatting and `git diff --check` passed.
- Planning-date fidelity checkpoint (T014 still open): a new SSR check failed on missing readable dates, and the browser check failed on the absent shared-calendar dialog before implementation. New Project now wraps the unchanged `DatePicker` in an opt-in native popover, uses the existing calendar icon/form utilities and a 180px width token, and retains raw ISO draft values. Clearing, focus restoration, Tab/Shift+Tab, Escape, one open calendar, reload and widths 390/768/1440 are exercised. Browser review reproduced a stale browsed month after reopening; a keyed fragment now remounts only the calendar, preserving draft input. Actual-app screenshots were inspected at 390 and 1440.
- Date checkpoint gates: 22 screen tests and nine navigation tests passed, along with strict server Clippy, WASM compilation, the full Dioxus build, explicit-file formatting and whitespace checks. The New Project browser workflow passed through real creation, recovery, concurrent edits, lost acknowledgements, conflicts and discard. No query, migration, dependency, imported data or existing preview changed. Shared CSS defaults and the Timesheet/gallery calendar implementation are unchanged; popover focus/positioning additions are explicitly opt-in.
- The combined browser run passed Projects but failed the responsive suite with the existing `dioxus-fullstack-0.7.9/src/magic.rs:267` response-body unwrap panic on `/`. A separate menu run passed all interaction assertions, including the new shared Timesheet calendar/week-selection regression, but still failed its no-page-errors gate with the same panic. These are failed suites, not green regressions; no errors were suppressed and the unapproved transport backport was not applied. Full-feature completion remains open.
- The remaining browser suites were then run independently and passed: mobile navigation, project bulk actions, bulk recovery, action errors and New Project (including focused selected dates at all three widths). This completes the checkpoint's executed coverage without turning the failed responsive/menu gates into passes. T014 and the overall 31/54 completion count remain unchanged.
- Configured progress now shares the alert evaluator in one authorized batch query, with explicit current-month/lifetime metadata and independent task/person allowances. The initial projection test failed at the missing API. Review then reproduced an assigned person without an allowance disappearing (one row instead of two); task/assignment identities now remain visible with absent budgets, without acquiring alert thresholds. Coverage includes current roles, deactivation, tenant isolation, independent projects, empty/unallocated scopes and unchanged lifetime totals. The full server-bin suite passed 673 tests (11 pre-existing ignored), core passed 109 and navigation passed 9. Two initial fixture failures (missing currency/rate mode) were corrected before the green runs.
- Projects preserves the three budget columns and shared styles, adds a native keyboard disclosure for scoped budgets and identifies the monthly period separately from total tracked hours. Integer display math preserves negative remaining hours, absent versus zero allowances and overflow safety. SQLx preparation against the isolated 36-migration database produced 798 records (seven new, only the replaced evaluator removed); strict server/core Clippy, WASM, the full Dioxus client/server build, targeted formatting and whitespace checks passed. No real data, migration, dependency, shared CSS or existing preview changed.
- T025 browser evidence: Projects design passed twice, including monthly-versus-lifetime values, scoped overruns, keyboard disclosure, widths 390/768/1440, pending/error/retry and unchanged shared defaults. The actual New Project creation-to-overview flow passed with a real authorized response (7,200-minute allowance, zero consumption), alongside recovery/conflict/discard checks. Actual-app budget screenshots were inspected at 390 and 1440. Mobile navigation passed; a focused run also passed bulk actions/recovery, action errors, menus and responsive layouts. Two combined default runs nevertheless failed on intermittent `unreachable` page errors (responsive first, menus second, the latter also missing an Edit heading); subsequent passes are not a transport fix. Menu/layout tests now retain URL/stack and panic diagnostics, without suppressing errors. A custom-order run put creation before bulk tests and correctly exposed their two-seed-project assumption; keep the existing default order. T025 is complete, while the full-feature stability gate and unapproved Dioxus backport remain open.
- Grouped report privacy: the new manager test first reproduced `cost_cents: 7777` from a private project override. The query now resolves the current active viewer and omits non-admin overrides before cost arithmetic. Any group containing restricted costs omits its cost rather than returning zero, a profile fallback or a partial sum. The UI labels affected rows and totals `Restricted`; billing/time figures remain unchanged. All four dimensions, mixed private/legacy groups, exact admin costs, zero overrides, role changes, deactivation and foreign filters are covered.
- Report regression passed all 667 server-bin tests (11 pre-existing ignored). The expanded draft ownership check then passed separately: a demoted administrator receives neither saved notes nor costs, and attempts to save/finalize those fields return forbidden without creating a project. T031 is complete; final browser/flake checks and the rest of the feature remain required. Detailed CSV/XLSX exports were inspected and contain no cost fields. No migration, dependency, shared CSS, imported data or preview changed.
- Report build gates passed: strict server Clippy, WASM compilation, targeted no-cache formatting and diff whitespace checks. SQLx preparation against the isolated 36-migration database produced 792 records, adding five queries and deleting only the replaced grouped-report query.
- Spec Kit prerequisites/plan/tasks setup executed in the isolated worktree. Requirements checklist 16/16; analysis mapped 20 functional requirements and 7 success criteria to 54 tasks without material gaps.
- Nix dev shell verified Rust 1.96.1, Cargo 1.96.1 and PostgreSQL 17.10 tools. Existing ignore rules cover target, worktrees and local database state. Browser runner inspected: creates its own temporary cluster; it has not been run for this feature yet.
- Core TDD red: 14 new checks failed at their unimplemented functions while 88 existing checks passed; a second increment had 3 expected new failures.
- Core green: `cargo test -p horae-core --quiet` passed 105 tests; `cargo clippy -p horae-core --all-targets -- -D warnings` passed; `cargo fmt --all` applied. Commands ran inside `nix develop` with the shared temporary target cache.
- Draft model tests passed (3). Schema tests first failed compilation against the baseline schema, then passed against migration 0030. An additional adversarial test reproduced a nullable fee-shape CHECK accepting an incomplete schedule; the CASE-based constraint fixed it. The combined focused `project_creation` run passed 8 tests (3 model + 5 database).
- The development PostgreSQL cluster is `/tmp/horae-new-project-db.XqwJiR`, socket-only, database `horae_new_project`. Its initial migration 0030 predates the fee CHECK correction; sqlx test databases receive the corrected current migration. Before applying subsequent migrations in development, create a fresh isolated database rather than rewriting the migration ledger or resetting real data.
- Explicit no-cache formatting covered all 13 new Rust/Spec Kit targets and passed without changes. Shared formatter configuration was not changed.
- Full server Clippy, WASM, browser and complete flake checks remain pending. The not-yet-connected DTOs currently produce dead-code warnings; do not suppress them instead of wiring the feature. No production/import database was changed.
- Backend increment: 26 focused tests passed, covering actor/client locks, private draft ownership, bounded raw input, concurrent saves, exact final-form validation, atomic creation/retry/rollback, project-only costs, task restrictions, currency mismatch, paginated catalogs and explicit client creation. An adversarial response-code test reproduced a 409 instead of 404 for unavailable foreign drafts; the corrected response now passes without disclosing ownership.
- A fresh database `horae_new_project_current` was created in the same isolated socket-only cluster and all 30 current migrations applied successfully. Use this database for subsequent commands; the earlier base is no longer needed for compilation.
- Cache regeneration on a warm shared target also requires recompiling the integration-test entry points: `touch crates/horae/tests/integration.rs crates/horae/tests/cli_restart.rs` changes timestamps only, then run `CARGO_INCREMENTAL=0 cargo sqlx prepare --workspace -- --features server --all-targets`. Verify no unrelated cached queries were deleted before committing.
- The draft persistence dependency was implemented before finalization so atomic/retry tests exercise a real saved draft. UI tasks remain pending; the new creation settings are not yet integrated into reporting, invoicing or task mutation enforcement. Project-created outbox records are written transactionally; their consumer remains pending.
- Verification of the backend checkpoint: all 26 focused checks passed; `SQLX_OFFLINE=true cargo clippy -p horae --features server -- -D warnings` passed after simplifying two nested conditionals. `cargo check -p horae --features web --target wasm32-unknown-unknown` passed with 18 unused-model warnings because the new screen is not wired yet. SQLx regenerated with zero deletions of existing cache entries. No shared CSS, utilities, existing pages or prototype files changed.
- Screen increment: `/projects/new` now has static routing, active Projects navigation and both creation links; the old inline creation branch was removed while keeping inline editing and bulk actions. The navigation regression failed before the link change and now passes alongside the other five detail-navigation checks. A production-route unit test also passes.
- The initial screen connects basic client/name/code/date/tag/currency/admin-note fields, a real client-creation dialog, acknowledged draft status, serialized coalescing saves, retry, cancel/back and confirmed discard. A scoped client lookup restores selected names/currencies beyond the first 50 catalog rows, preserves archived identity and rejects foreign access/role changes; the focused backend suite now passes 27 checks.
- Draft-state TDD reproduced missing request/acknowledgement handling before implementation. Five pure state tests cover initial status, immutable retry payloads, later edits staying dirty, mismatched acknowledgements, validation correction and recovery identity. Production-screen rendering tests cover labels, no fabricated defaults/spurious saves, client recovery and administrator-only notes. These are not browser interaction tests.
- SQLx cache regeneration now contains 641 entries (one new selected-client query, zero existing deletions). Server Clippy and WASM compilation passed without the earlier unused-model warnings. New CSS only introduces two size tokens and the scoped `np-*` layout; no shared component styles or generated utilities were changed.
- T014/T019/T048 are deliberately still open: native date/select controls still need the reviewed shared-picker integration, browser navigation protection/recovery must be exercised, and the full responsive design is not yet present. Billing, tasks/team, invoice defaults, downstream consumers, notification dispatch, browser/flake gates and the final PR remain required. This is an implementation checkpoint, not a finished feature or a reduced scope.
- Final screen-checkpoint rerun: `new_project_screen` passed 12 checks (rendering, state and DTO checks), `detail_navigation` passed 6, strict server Clippy and WASM check both passed without warnings. A definite creation rejection now unlocks corrections, while uncertain completion retains the identity for retry; archived/unavailable clients cannot enable Save project. Full browser interaction remains unverified.
- Conditional-panel increment: production components now expose all three project types, person/task/project rates, single/milestone/monthly fees, supported budget scopes, task restrictions, project memberships/leads, visibility and invoice defaults. Existing controls/tokens are reused; the four additional CSS rules are scoped to New Project. Email alerts remain honestly unavailable until the planned transport integration exists.
- Rendering/helper TDD: the new conditional-panel assertions and team helpers failed before implementation, then the expanded `new_project_screen` suite passed 20 checks. Add everyone paginates the real active catalog, deduplicates memberships, preserves overrides and rejects an oversized result without partial changes; creation/navigation are disabled during that fetch. Removing a teammate also removes their task-specific grants, preserving an intentionally empty restricted task.
- A regression test reproduced hidden invalid invoice inputs blocking a non-billable project. Validation now ignores those inputs without erasing the draft and persists neutral invoice defaults (including an empty PO number). All 28 focused `project_creation` checks passed. SQLx metadata was regenerated against the isolated database; all 641 entries remained unchanged.
- Browser scenarios use `new-project.cjs`, following the repository's existing executable JavaScript convention rather than adding a TypeScript runner. `run-design-checks.sh new-project` selects that suite while retaining the disposable database and reserved-port checks. The default run includes New Project after the existing suites. No runtime dependencies or shared-control changes were introduced.
- Isolated browser checks passed real client creation, restored dates/billing/team/task restrictions/taxes, conditional type switching, Cancel recovery, edits during an in-flight save, lost-save acknowledgement with newer edits, idempotent finalization after a lost response, stale-tab conflicts/reload and confirmed discard. Widths 390/768/1440 fit without document overflow. Actual-app screenshots at those widths were captured; native picker fidelity and complete visual acceptance remain open.
- Existing Projects design, all responsive headers, menus, mobile navigation and bulk-action checks passed. Bulk recovery initially hit a login timeout and passed all three cases on rerun. The action-error suite was updated to use the retained project edit form and passed all scenarios; the existing edit button label remains `Cancel`. One extended New Project run reported a WASM `unreachable` error after its assertions; an instrumented rerun passed without a page error. Keep this intermittent failure visible until the repeated final browser gate is stable.
- These are frontend/persistence checks, not proof of downstream financial or permission behavior. Report/invoice/budget consumers, task-mutation enforcement, notification delivery, selected task/person recovery beyond the first catalog page, navigation protection, full visual/accessibility gates and the final PR still remain required. No task is considered complete solely because its controls now render.
- Instrumentation reproduced the intermittent error at `dioxus-fullstack-0.7.9/src/magic.rs`: its response decoder unwraps a failed body read. Browser tests now await outstanding API responses before destroying documents/tabs; deliberate interrupted-save/finalization cases remain unchanged. The complete New Project scenario passed with this teardown sequencing and all page errors still asserted absent. No shared component or dependency was patched; arbitrary transport failures during body decoding remain an upstream risk for final review.
- Increment verification: 20 screen checks, 6 navigation checks, 28 creation checks, strict server Clippy, WASM compilation and the full Dioxus server/client build passed. Formatting and `git diff --check` passed. The design-source PR #206 is still open; the future implementation PR must account for that base rather than silently assuming it has merged.
- Final integrated browser rerun passed all eight suites: Projects design, responsive layouts, menu popovers, mobile navigation, bulk actions, bulk recovery, action errors and New Project. New Project also passed independently after the teardown fix; no page errors were ignored. T020 and T029 are checked; navigation protection (T019), downstream financial integration and full visual/accessibility acceptance are still open.
- Catalog recovery increment: a production-screen test first failed because selected tasks and teammates outside the initial catalog page rendered as unavailable. A bounded selected-ID endpoint now resolves only missing active same-org identities, deduplicates IDs in SQL and excludes costs from manager responses. Initial loading enriches display options without changing or resaving the draft. All 30 focused creation checks and 21 screen checks passed, including inactive/foreign records, request limits, role changes and serialized cost redaction.
- SQLx preparation added two selected-catalog queries and one isolated test-fixture query (644 entries, zero existing deletions). Strict server Clippy, WASM compilation and the full server/client build passed. Browser assertions now inspect the real selected-ID response while catalog pages omit the chosen task/person, then verify restored names and project-lead selection. The New Project scenario passed; the first combined run again exposed the upstream body-decoder panic during an initial hard navigation, so the test now enters Projects through its actual navigation link. No page errors are suppressed and dependency-level resilience remains part of final review.
- The repeated combined Projects-design/New-Project browser run passed, including the explicit selected-ID response and all save/conflict/discard recovery checks. T038's frontend controls are complete, including selection recovery; their downstream permission/financial enforcement and role-matrix gates remain open. No CSS, shared component defaults or production data changed in this increment.
- Transport regression: `run-design-checks.sh new-project-transport` reaches the response body after a real successful draft save, then reproduces `RuntimeError: unreachable`; the form remains at `Saving draft…` instead of offering retry. Earlier harness attempts failed before body decoding and were corrected; they are not evidence of this regression. The focused test is intentionally RED and is not yet in the default runner list. After the compatible fix, require it to pass and add it to the default gate. T019/T051 stay open; no runtime source, dependency, shared CSS or real database was modified.
- Project-event delivery increment: new tests first failed at the absent kind-scoped claim, confirmed plugin dispatch and outbox consumer. A subsequent adversarial runtime test showed fail-fast plugin delivery suppressing a healthy later subscriber; delivery now attempts every subscriber before returning failure. All 47 `jobs::`, 64 `plugin::` and 30 `project_creation` tests pass, including real WASM failure/recovery, capacity limits, acknowledgement waiting, lease fencing/backoff, worker shutdown and the real concurrent-finalization-to-outbox path. T018 is complete; this does not complete optional email delivery or the pending transport/navigation work. The Dioxus backport remains unapproved and no dependency changes have been made.
- Delivery verification: SQLx preparation succeeded against the isolated migrated database, yielding 648 entries (five new entries and removal of only the replaced unscoped claim query). Strict server Clippy and WASM compilation passed; targeted no-cache formatting and `git diff --check` passed. No migration, shared CSS or design-source change was required. A full application/browser/flake rerun remains part of the final gate, and the separately reproducible response-body regression remains unresolved.
- Hourly billing checkpoint: a new end-to-end fixture first reproduced person mode incorrectly using the task rate (5000 instead of 4000 cents). The SQL resolver and all five operational queries now agree across person/task/project modes, absent sources and explicit zero. SQL/Rust parity covers 1,215 combinations. Fixed-fee configurations no longer charge hours, while legacy fixed-fee/cascade fixtures retain their previous amounts. Mixed billing currencies reject the whole invoice transaction; configured USD billing and EUR project-specific costs remain separately denominated.
- Financial privacy regression: the member's own Harvest entry initially exposed both its 3000-cent bill rate and 1500-cent project cost. Both detail and collection responses now omit these financial values for members while keeping worked hours and manager access. The first broader invoice run also caught a missing assignment in a new cost fixture; that fixture was corrected before rerunning. All 84 focused invoice/project/report/Harvest/plugin database checks passed, followed by 105 core tests and 597 server-bin tests (11 pre-existing ignored checks), with no failures.
- Migration 0031 has been applied only to `horae_new_project_current` in the isolated socket-only cluster. It currently contains the rate helper and PUBLIC privilege revocation, not the pending fee/invoice schema. If that unshipped migration is expanded, prepare a fresh isolated compilation database; do not rewrite migration checksums in an existing ledger. No shared CSS, design assets, dependencies or real imported database were changed.
- Hourly verification gates: SQLx regeneration succeeded with 668 entries (26 new queries and six replaced queries removed); strict server Clippy and WASM compilation passed. Explicit no-cache formatting passed for all eleven changed Rust/Spec Kit files, and `git diff --check` passed. This checkpoint has not rebuilt the Dioxus preview or rerun browser/flake checks; the previously recorded truncated-response regression remains pending approval of the compatible fix.
- Fee lifecycle RED: a single configured fee with no time returned 404 (`No billable, un-invoiced time...`). Generation now prepares single, milestone and monthly occurrences, inserts source-exclusive invoice lines, atomically claims fees and releases claims on void without rewriting history. The UI and CSV/XLSX/PDF paths handle absent hours/rates explicitly. The invoice form explains that it creates a draft including eligible fees and never sends automatically.
- A new isolated database `horae_new_project_fees` was created in `/tmp/horae-new-project-db.XqwJiR` and all 31 migrations applied, including the expanded unshipped 0031. Use `postgres://postgres@localhost/horae_new_project_fees?host=/tmp/horae-new-project-db.XqwJiR` for subsequent preparation/builds; the older `horae_new_project_current` has the earlier 0031 checksum. Neither database's migration ledger was rewritten, and the real imported database was untouched.
- Fee verification: 609 server-bin tests passed (11 pre-existing ignored), including exclusive-source/FK constraints, no synthetic time, all three fee schedules, leap February, partial periods/project end, overdue/future milestones, concurrent generation, repeat claims, overflow rollback and void/rebilling identity. Actual CSV output has empty fee hours/rates; inspected XLSX cells are absent rather than zero; real Typst PDF compilation is deterministic with fee rows. Existing hourly/import/report/plugin regressions also pass. Project/default selection, invoice adjustment snapshots, final browser review and the transport fix remain open.
- Fee gates: SQLx preparation succeeded against `horae_new_project_fees` (693 entries; 28 new, three replaced queries removed, one existing nullability description updated). Strict server Clippy, WASM compilation, targeted no-cache formatting of fifteen Rust/Spec Kit files and `git diff --check` passed. T022/T026 are complete; this does not complete invoice defaults/adjustments or the final browser/flake gates. No application preview was restarted and no dependency or shared CSS was changed.
- Budget TDD: the new threshold tests initially failed at the missing helper; integration tests likewise failed before the evaluator/notification schema existed. The implemented evaluator covers project hours/fees, task hours/fees, person hours, monthly/non-billable inclusion and locked rounding. All 108 core tests and 15 focused budget integration tests passed, including leap February, tenant/recipient isolation, exact zero rates, concurrent checks, repeated crossings, independent months, periodic recovery and notification/outbox rollback. The notification schema has no PUBLIC grant and outbox payloads contain only the notification ID.
- Migration 0032 was applied additively to the same isolated `horae_new_project_fees` database (32 migrations); no prior migration checksum or real data was changed. Post-write checks and the minute scheduler now use the configured evaluator while legacy projects retain their hours/plugin-band path. Notifications remain pending jobs, not delivered email. T024 is complete; T025 remains open for authorized/displayed project progress, and T027/T028 still own delivery. Full browser/flake gates and the unapproved Dioxus transport fix remain outstanding.
- Budget regression gates: the final batched-enqueue implementation passed all 623 server-bin tests (11 pre-existing ignored). SQLx preparation succeeded with 741 entries (48 new, zero existing modifications/deletions); strict core/server Clippy and WASM compilation passed. Targeted no-cache formatting passed for all nine Rust/Spec Kit files without changes, and `git diff --check` passed. No shared CSS, design sources, dependency or running preview was changed. This increment does not claim a browser or full flake rerun.
- Mail TDD: transport tests first failed at the missing configuration/render/send functions; queue tests then failed before the delivery consumer and terminal schema existed. Temporary executable stubs verify fixed arguments, injection rejection, bounded execution/output, cancellation, stable identity, retry recovery, five-attempt exhaustion, revoked/foreign recipients, stale leases, disabled configuration and kind-scoped shutdown. An additional adversarial test reproduced overlong body lines; bounded Unicode wrapping fixed it. No real mail transport was executed.
- Mail verification: all 641 server-bin tests passed (11 pre-existing ignored), including 17 notification tests and environment configuration coverage. Migration 0033 was applied only to the isolated `horae_new_project_fees` database; SQLx preparation succeeded with 752 entries (14 new and three replaced outbox queries removed). Strict server Clippy and WASM compilation passed. The browser runner now clears deployment mail variables before starting fixtures. T027/T028 are complete; project progress/privacy, final browser/flake gates and the unapproved Dioxus response-body fix remain open.
- Mail finishing checks: targeted no-cache formatting passed for all 16 changed Rust/shell/Spec Kit files with zero changes; shell syntax and `git diff --check` passed. No dependency, design asset, shared CSS, real imported database or running preview changed. This checkpoint does not claim visual or full flake verification.
- Task restriction TDD: three new checks reproduced selectable/trackable empty restricted tasks, accepted edits (including unchanged input), and accepted reordering. A shared transactional guard and additive migration 0034 now cover interactive creation/start, update, reschedule, reorder and delete. The first focused Timesheet run passed 35 checks, including a real blocked-edit/grant-revocation race and positive tracking/archived-history checks for member, manager and administrator roles. Stopping an own running timer remains possible after revocation, without bypassing ownership or locked-state checks.
- Expanded restriction verification adds concurrent restriction-toggle/grant-deletion checks during manual/timer insertion, a mixed allowed/restricted batch rollback, and the timer open-state guard. The first full run passed 649 tests but failed while constructing the new batch fixture because its project-task billability was missing; the fixture was corrected before rerunning. Migration 0034 was applied only to the isolated `horae_new_project_fees` database (34 migrations); no prior checksum or real imported data changed.
- Restriction regression rerun: all 650 server-bin tests passed (11 pre-existing ignored), including the nine new restriction checks and existing approval/submission/import regressions. T033/T034 are complete. This verifies the backend behavior, not the outstanding browser role matrix or project/report/export privacy work; T031/T035–039 remain tracked separately.
- Restriction build gates: SQLx preparation succeeded with 766 cache entries (14 new, zero existing modifications/deletions); strict server Clippy and WASM compilation passed. Targeted no-cache formatting covered the six changed Rust/Spec Kit files with zero changes, and `git diff --check` passed. No shared CSS, design sources, dependencies or running preview changed. The final browser/flake gates and the unapproved Dioxus response-body fix remain outstanding.
- Project read privacy: initial tests failed at the missing scoped read helpers and actor-bound spend signature. The first green focused run passed 43 Projects tests. Additional legacy and serialized-response recovery assertions then passed with the full server-bin suite: 656 passed, 11 pre-existing ignored, no failures. The six new read-boundary checks cover member-visible/managers-only progress, project leads/admins, current database authority, organization isolation, outsiders/inactive users, revoked membership, archived identity and legacy compatibility.
- Migration 0035 was applied only to the isolated `horae_new_project_fees` database (35 migrations). Its private read view now gates Projects list/spend/team reads; task reads redact member rates. Timesheet and Timer use separate rate-free identity resources, preserving own archived labels without broadening the active task catalog. T035 is complete; configured budget display, export/Harvest/cost privacy and browser role verification remain open. No CSS, dependency, real imported data or running preview changed.
- Read-boundary build gates: the first all-target SQLx preparation exposed the standalone TimerWidget fixture's old resource function names. Updating those stubs restored preparation and its production-widget test passed. The regenerated cache has 776 entries (15 new and only the five replaced read queries removed). Strict server Clippy and WASM compilation passed. Targeted no-cache formatting and `git diff --check` passed. This does not claim a browser/full-flake rerun; the unapproved Dioxus response-body fix remains outstanding.
- Project export/compatibility TDD: two new export tests reproduced a private budget in CSV and an unauthorized overlong project causing XLSX preflight to return 413. A real-session Harvest router test then reproduced two hidden projects in pagination counts. CSV, XLSX preflight/payload, and Harvest project count/page/detail now reuse `project_read_access` with the current actor. The existing export snapshot, bounded-size and streaming behavior is retained; own time-entry identities remain accessible without progress permission.
- Export/compatibility regression: all 659 server-bin tests passed (11 pre-existing ignored), including the three new checks, member/lead/manager/admin routes, role changes, deactivation, revoked membership, archived scope, tenant isolation, allowed budgets, real CSV output and XLSX generation. Legacy member fixtures now explicitly assign their viewer. T036 remains open for Harvest tasks and complete time-entry privacy; no new migration, CSS, dependency, imported-data mutation or preview restart was required.
- Task/time-entry privacy TDD reproduced unauthorized task counts/rates, private budget metadata on personal time, and a manager reading an administrator-only project cost. Migration 0036 now shares catalog identity access between the UI and Harvest, while entry queries recheck current actor scope and omit inaccessible metadata. A stale-role test also verifies that changing a caller DTO cannot promote the database user. Migration applied only to the isolated 36-migration database; no imported data or preview changed.
- Privacy regression passed all 663 server-bin tests (11 pre-existing ignored), including plugin rejection of deliberately misconfigured access to all 14 configuration tables/views and the private rate resolver. The actual project-created outbox payload contains only the existing public fields despite a form with private notes/costs; the additional membership payload test passed separately. T036/T037 are complete. Broader report cost privacy, browser role verification and final feature gates remain open.
- Increment gates: server Clippy with warnings denied, WASM check, explicit-file treefmt and `git diff --check` passed. SQLx preparation used `--workspace -- --features server --all-targets` against the isolated database: 788 cache records, replacing exactly seven changed queries and adding their replacements plus new test queries. No dependency or shared CSS changes.
- Export/compatibility build gates: SQLx preparation succeeded against the isolated 35-migration database with 780 cache entries (nine new, only the five replaced project/export queries removed). Strict server Clippy, WASM compilation, targeted no-cache formatting of eleven Rust/contract/task files and `git diff --check` passed. Final browser/flake verification and the unapproved response-body backport remain outstanding.
