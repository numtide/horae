target/
.dev-vm.qcow2
nixos.qcow2
*result*

# Local dev stack state (process-compose: postgres data dir)
/.data/

# git worktrees
/.worktrees/

# direnv
.direnv/
.envrc.local

# Design bundle thumbnail/screenshot captures (not source)
design/**/.thumbnail

# Local scratch space (analysis notes, references — not versioned)
.scratch/
